# Python
__pycache__/
*.py[cod]
*.pyo
.env

# SAM.gov daily response cache (auto-generated, ephemeral)
.cache/

# Runtime state files are intentionally tracked — seen_items.json persists
# deduplication state between GitHub Actions runs; feedback_log.json stores
# team scoring calibration. Do not add these to .gitignore.
# seen_items.json  ← tracked on purpose
# feedback_log.json ← tracked on purpose
